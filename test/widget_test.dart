import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // Supabase initialize gerektiren widget testleri ayrıca kurulmalı
    expect(true, isTrue);
  });
}
